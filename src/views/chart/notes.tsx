import ChartNotes from 'features/notes';
import React, { FC } from 'react';

/**
 * @view ChartNotes
 */
const ChartNotesView: FC = () => {
  return <ChartNotes />;
};
ChartNotesView.displayName = 'ChartNotesView';

export default ChartNotesView;
